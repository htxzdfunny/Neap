# Neap
A simple TOTP authenticator.

# build
```bash
flutter build apk --split-debug-info --obfuscate  --split-per-abi #(--build-number 72)
```
# Todo
* sort
* quick mode(show code in homepage)
* more feature...